import { AuthService } from "./auth.service.duck";

@Controller("/auth")
func AuthController {
    authService: AuthService;

    @POST("/login")
    async login(): string {
        return "logged in";
    }
}
