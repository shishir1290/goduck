package routes

import (
	"{{ .Module }}/app/Controllers"

	"github.com/shishir1290/goduck/runtime/server"
)

func RegisterRoutes(app *server.Server) {

{{- range .Routes }}
	app.{{ .MethodUpper }}("{{ .Path }}", Controllers.{{ .Controller }}{{ .Action }})
{{- end }}

}